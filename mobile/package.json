{
  "name": "libretv-ios",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor iOS shell for LibreTV — a WebView app that loads your deployed LibreTV site.",
  "scripts": {
    "add:ios": "cap add ios",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
